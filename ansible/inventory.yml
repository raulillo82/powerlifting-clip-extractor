all:
  vars:
    ansible_user: raul
    app_dir: /home/raul/powerlifting-clip-extractor
    repo_url: https://github.com/raulillo82/powerlifting-clip-extractor.git

  hosts:
    rpi5:
      ansible_host: raspberrypi5   # production (current)

    rpi4:
      ansible_host: raspberrypi4   # failover target — update hostname/IP if needed
